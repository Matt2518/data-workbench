'use strict';
// Plugin manifest for the reshape category.
// Add new plugin filenames here to include them in the build.
module.exports = {
  category: 'Reshape',
  plugins: [
    './unpivot.js'
  ]
};
